<PageTitle>Login</PageTitle>

<div class="row justify-content-center">
    <div class="col-md-6">
        <div class="card">
            <div class="card-header">
                <h3 class="text-center">Login</h3>
            </div>
            <div class="card-body">
                <EditForm Model="@loginModel" OnValidSubmit="HandleLogin">
                    <DataAnnotationsValidator/>
                    <ValidationSummary/>

                    <div class="form-group mb-3">
                        <label for="employee">Select Employee</label>
                        <InputSelect data-testid="@Elements.User" @bind-Value="loginModel.Username" class="form-control">
                            <option value="">-- Select an employee --</option>
                            @foreach (var employee in employees)
                            {
                                <option value="@employee.UserName">@employee.GetFullName()</option>
                            }
                        </InputSelect>
                        <ValidationMessage For="@(() => loginModel.Username)"/>
                    </div>

                    @if (!string.IsNullOrEmpty(errorMessage))
                    {
                        <div class="alert alert-danger">@errorMessage</div>
                    }

                    <div class="text-center">
                        <button data-testid="@Elements.LoginButton" type="submit" class="btn btn-primary">Login</button>
                    </div>
                </EditForm>
            </div>
        </div>
    </div>
</div>

@code {

    public enum Elements
    {
        User,
        LoginButton
    }
}
